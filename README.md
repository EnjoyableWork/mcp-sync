# mcp-sync
The deterministic, zero-latency MCP configuration engine. Auto-detects local AI clients, validates server health via STDIO JSON-RPC, and synchronizes server definitions across Claude Desktop, Cursor, Windsurf, and VS Code in &lt;1.2s with atomic .bak safety rollbacks.
