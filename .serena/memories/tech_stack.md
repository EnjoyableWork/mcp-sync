# Tech stack
- No source language, runtime, package manager metadata, dependencies, or build configuration is present.
- README describes a future/distributed CLI invoked as `mcp-sync` or through `npx`; do not infer an implemented Node.js package from documentation alone.
- Repository artifacts are Markdown documentation and an MIT license.
