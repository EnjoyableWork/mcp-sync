# Suggested commands
- No repository-defined development, build, test, lint, format, or run command
  exists because `MCP-002` has not created the Rust source tree or Cargo
  manifest yet.
- README CLI commands are target product examples, not locally runnable project
  scripts.
- For documentation-only changes, use the checks in `mem:task_completion`.
