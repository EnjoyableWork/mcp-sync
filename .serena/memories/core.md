# Project map
- Repository is currently documentation-only: `README.md` plus MIT `LICENSE`; no implementation, package manifest, or test configuration exists yet.
- Product name is lowercase `mcp-sync`.
- Read `mem:tech_stack` for the current implementation/tooling boundary.
- Read `mem:conventions` for repository-specific documentation conventions.
- Read `mem:suggested_commands` for available project commands.
- Read `mem:task_completion` for completion checks.
